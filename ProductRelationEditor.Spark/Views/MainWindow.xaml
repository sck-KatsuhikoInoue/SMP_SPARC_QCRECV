<UserControl x:Class="ProductRelationEditor.Spark.Views.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:v="clr-namespace:ProductRelationEditor.Spark.Views"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="データ取得" Width="120" Command="{Binding FetchDataCommand}"/>
            <Button Content="登録" Width="120" Command="{Binding RegisterDataCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item1" Binding="{Binding Item1}" />
                <DataGridTextColumn Header="Item2" Binding="{Binding Item2}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>

</UserControl>

